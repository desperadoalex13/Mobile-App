import 'package:flutter_test/flutter_test.dart';

// Smoke test — verifies the test runner itself is functional.
// Full feature coverage is in the individual test files under test/.
void main() {
  test('test runner is functional', () {
    expect(1 + 1, 2);
  });
}
