import 'package:flutter/material.dart';

class DishLibraryScreen extends StatelessWidget {
  const DishLibraryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Dish Library')),
      body: const Center(child: Text('Dish Library — coming soon')),
    );
  }
}
